# SFUMATO Reference Project Configuration
# ==============================================================================
# In the Theme section, make changes and remove any settings for which defaults
# will be used to keep this file manageable.
# ==============================================================================

# One or more project paths:
# - Path: relative to the location of this file
# - Extensions: comma separated list of file extensions to watch/process
# - Recurse: watch/process all subdirectories
ProjectPaths:

    - Path: wwwroot
      Extensions: html,htm
      Recurse: false

    - Path: wwwroot/pages
      Extensions: html,htm
      Recurse: true

    - Path: wwwroot/stylesheets
      Extensions: scss
      Recurse: true

# Dark theme options:
#   - class: html tag class "theme-dark" triggers dark mode styles
#   - media: system theme triggers dark mode styles
DarkMode: class

# Use with DarkMode:class; use html tag class "theme-auto" to trigger dark mode styles
# with the system theme
UseAutoTheme: true

# This example shows all available option groups.
# Include one or more to add custom options or replace existing ones.
# The Theme item or any of its option groups can be omitted.
# * Options use SCSS values
Theme:
    
    BackgroundImage:
        
        # This option exists so it will be replaced 
        gradient-to-t: linear-gradient(to top, var(--sf-gradient-stops))

    BackgroundPosition:
        
        # This option exists so it will be replaced 
        right-top: right top

    BackgroundSize:
        
        # This option exists so it will be replaced 
        cover: cover

    FromPosition:
        
        # This option does not exist so it will be added 
        hero: 25em

    ToPosition:
        
        # This option does not exist so it will be added 
        hero: 50em

    ViaPosition:
        
        # This option does not exist so it will be added 
        hero: 90em

    BorderWidth:
        
        # This option does not exist so it will be added 
        hero: 1.25rem

    BorderRadius:
        
        # This option does not exist so it will be added 
        hero: 2rem

    BoxShadow:
        
        # This option does not exist so it will be added 
        panel: 0 1px 2px 0 rgb(0 0 0 / 0.05);
        
    FilterSize:
        
        # This option does not exist so it will be added 
        panel: 2rem
        
    BackdropGrayscale:
        
        # This option does not exist so it will be added 
        panel: 75%
        
    BackdropHueRotate:
        
        # This option does not exist so it will be added 
        panel: 13deg
    
    BackdropInvert:
        
        # This option does not exist so it will be added 
        panel: 75%
    
    BackdropSepia:
        
        # This option does not exist so it will be added 
        panel: 75%
    
    Grayscale:
        
        # This option does not exist so it will be added 
        panel: 75%
    
    HueRotate:
        
        # This option does not exist so it will be added 
        panel: 13deg
    
    Invert:
        
        # This option does not exist so it will be added 
        panel: 75%
    
    Sepia:
        
        # This option does not exist so it will be added 
        panel: 75%
    
    GridAutoCols:

        # This option does not exist so it will be added 
        hero: minmax(0, 100em)

    GridAutoRows:

        # This option does not exist so it will be added 
        hero: minmax(0, 100em)

    GridCol:

        # This option does not exist so it will be added 
        full-row: 1 / -1

    Gap:

        # This option does not exist so it will be added 
        newspaper: 1.5rem

    GridCols:

        # This option does not exist so it will be added 
        newspaper: 1fr 1fr 1fr

    GridRows:

        # This option does not exist so it will be added 
        newspaper: 1fr 1fr 1fr

    Order:

        # This option does not exist so it will be added 
        advertisement: 3

    GridRowEnd:

        # This option does not exist so it will be added 
        last: -1

    GridRowSpan:

        # This option does not exist so it will be added 
        last: 3 / -1

    GridRowStart:

        # This option does not exist so it will be added 
        first: 1

    FlexBasis:

        # This option does not exist so it will be added 
        newspaper: 1fr

    Flex:

        # This option does not exist so it will be added 
        newspaper: 1

    FlexGrow:

        # This option does not exist so it will be added 
        use-grow: 1

    FlexShrink:

        # This option does not exist so it will be added 
        use-shrink: 1

    Cursor:

        # This option does not exist so it will be added 
        cat: url(/images/cat.png), 4 12, pointer

    PointerEvents:

        # This option does not exist so it will be added 
        fill: fill

    ScrollMargin:

        # This option does not exist so it will be added 
        feature: 2rem

    ScrollPadding:

        # This option does not exist so it will be added 
        feature: 2rem

    UserSelect:

        # This option does not exist so it will be added 
        paragraph: revert

    AspectRatio:
        
        # This option exists so it will be replaced 
        video: 16 / 9

    Bottom:

        # This option does not exist so it will be added 
        tight: 0.5rem

    Box:

        # This option does not exist so it will be added 
        initial: initial

    BoxDecoration:

        # This option does not exist so it will be added 
        initial: initial

    BreakAfter:

        # This option does not exist so it will be added 
        initial: initial

    BreakBefore:

        # This option does not exist so it will be added 
        initial: initial

    BreakInside:

        # This option does not exist so it will be added 
        initial: initial

    Clear:

        # This option does not exist so it will be added 
        initial: initial

    Columns:

        # This option does not exist so it will be added 
        8xl: 100rem

    Container:
      
      # This option exists so it will be replaced 
      "": "width: 100%;
           margin-left: auto;
           margin-right: auto;
            
           @include sf-media($from: $sm-breakpoint) {
               max-width: $sm-breakpoint;
           }
            
           @include sf-media($from: $md-breakpoint) {
               max-width: $md-breakpoint;
           }
            
           @include sf-media($from: $lg-breakpoint) {
               max-width: $lg-breakpoint;
           }
            
           @include sf-media($from: $xl-breakpoint) {
               max-width: $xl-breakpoint;
           }
            
           @include sf-media($from: $xxl-breakpoint) {
              max-width: $xxl-breakpoint;
           }"

    Elastic:

      # This option exists so it will be replaced 
      "": "width: 100%;
           margin-left: auto;
           margin-right: auto;
           
           @include sf-media($from: $sm-breakpoint) {
               max-width: calc(#{$md-breakpoint} - 1px);
           }
           
           @include sf-media($from: $md-breakpoint) {
               max-width: calc(#{$lg-breakpoint} - 1px);
           }
           
           @include sf-media($from: $lg-breakpoint) {
               max-width: calc(#{$xl-breakpoint} - 1px);
           }
           
           @include sf-media($from: $xl-breakpoint) {
               max-width: calc(#{$xxl-breakpoint} - 1px);
           }
           
           @include sf-media($from: $xxl-breakpoint) {
               max-width: $xxl-breakpoint;
           }"

    InsetEnd:

        # This option does not exist so it will be added 
        margin: 1rem

    InsetStart:

        # This option does not exist so it will be added 
        margin: 1rem

    Inset:

        # This option does not exist so it will be added 
        margin: 1rem

    InsetX:

        # This option does not exist so it will be added 
        margin: 1rem

    InsetY:

        # This option does not exist so it will be added 
        margin: 1rem

    Isolate:

        # This option does not exist so it will be added 
        initial: initial

    Left:

        # This option does not exist so it will be added 
        start: 2rem

    Right:

        # This option does not exist so it will be added 
        start: 2rem

    Top:

        # This option does not exist so it will be added 
        start: 2rem

    ZIndex:

        # This option does not exist so it will be added 
        topmost: 9999

    Height:

        # This option does not exist so it will be added 
        banner: 12em

    MaxHeight:

        # This option does not exist so it will be added 
        banner: 12em

    MaxWidth:

        # This option does not exist so it will be added 
        banner: 60em

    MinHeight:

        # This option does not exist so it will be added 
        banner: 12em

    MinWidth:

        # This option does not exist so it will be added 
        banner: 60em

    Width:

        # This option does not exist so it will be added 
        banner: 60em

    Margin:

        # This option does not exist so it will be added 
        banner: 1.25rem

    Padding:

        # This option does not exist so it will be added 
        banner: 1.5rem

    SpaceX:

        # This option does not exist so it will be added 
        banner: 1rem

    SpaceY:

        # This option does not exist so it will be added 
        banner: 1rem

    SvgStroke:

        # This option does not exist so it will be added 
        button: 0.25em

    BorderSpacing:

        # This option does not exist so it will be added 
        data-cell: 0.25em

    BorderSpacingX:

        # This option does not exist so it will be added 
        data-cell: 0.25em

    BorderSpacingY:

        # This option does not exist so it will be added 
        data-cell: 0.25em

    CaptionSide:

        # This option does not exist so it will be added 
        initial: initial

    Animate:
        
        # This option exists so it will be replaced 
        spin: |
            animation: spin 1s linear infinite;
            
            @keyframes spin {
                from {
                    transform: rotate(0deg);
                }
                to {
                    transform: rotate(360deg);
                }
            }

    TransitionDelay:

        # This option does not exist so it will be added 
        spinner: 10ms

    TransitionDuration:

        # This option does not exist so it will be added 
        spinner: 500ms

    TransitionTiming:

        # This option does not exist so it will be added 
        spinner: cubic-bezier(0.4, 0, 1, 1)

    Transition:
        
        # This option exists so it will be replaced 
        color: |
            transition-property: color, background-color, border-color, text-decoration-color, fill, stroke;
            transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
            transition-duration: 150ms;

    Origin:

        # This option does not exist so it will be added 
        logo: 3em 3em

    Rotate:

        # This option does not exist so it will be added 
        logo: 15deg

    Scale:

        # This option does not exist so it will be added 
        logo: 1.25

    ScaleX:

        # This option does not exist so it will be added 
        logo: 1.25

    ScaleY:

        # This option does not exist so it will be added 
        logo: 1.25

    SkewX:

        # This option does not exist so it will be added 
        logo: 15deg

    SkewY:

        # This option does not exist so it will be added 
        logo: 15deg

    TranslateX:

        # This option does not exist so it will be added 
        center: 50%

    TranslateY:

        # This option does not exist so it will be added 
        center: 50%

    DecorationThickness:

        # This option does not exist so it will be added 
        feature: 1em

    FontFamily:

        # This option does not exist so it will be added 
        blog: "\"San Francisco\", Helvetica, sans-serif"

    FontWeight:

        # This option does not exist so it will be added 
        headline: 600

    Leading:

        # This option does not exist so it will be added 
        headline: 1.25

    LineClamp:

        # This option does not exist so it will be added 
        headline: 10

    ListStylePosition:

        # This option does not exist so it will be added 
        initial: initial

    ListStyleType:

        # This option does not exist so it will be added 
        initial: initial

    ListStyleImage:

        # This option does not exist so it will be added 
        initial: initial

    TextAlign:

        # This option does not exist so it will be added 
        justify-all: justify-all

    TextSize:

        # This option does not exist so it will be added 
        footnote: 0.75rem

    Tracking:

        # This option does not exist so it will be added 
        footnote: 0.015em

    UnderlineOffset:

        # This option does not exist so it will be added 
        caption: 3px

    # Override media breakpoints;
    # Using em units is ideal because they are the most reliable across
    # browsers and devices.
    #   - Sm => tablet (portrait, small)  - 640px
    #   - Md => tablet (portrait)         - 768px
    #   - Lg => tablet (landscape)        - 1024px
    #   - Xl => desktop                   - 1280px
    #   - Xxl => elastic                  - 1536px
    #     Stops scaling at this breakpoint when using vw font size units
    #     (see FontSizeUnit below)
    # * These are the default values
    MediaBreakpoint:
        Sm: 640px
        Md: 768px
        Lg: 1024px
        Xl: 1280px
        Xxl: 1536px

    # Override breakpoint font sizes;
    # Default value for breakpoint base font size is 16px.
    # Scaling solutions can use vw units, like "4.35vw" or "1vw".
    # When using vw units, the xxl breakpoint will freeze scaling.
    # * These are the default values so this section can be removed.
    FontSizeUnit:
        Zero: 16px
        Sm: 16px
        Md: 16px
        Lg: 16px
        Xl: 16px
        Xxl: 16px

    # This example is 5 new colors named after the Dieter Rams Braun 05
    # color palette so they will be added as color options.
    Color:
        dr05a: "#af2e1b"
        dr05b: "#cc6324"
        dr05c: "#3b4b59"
        dr05d: "#bfa07a"
        dr05e: "#d9c3b0"
